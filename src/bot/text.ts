import { InlineKeyboard } from "grammy";

/**
 * متن‌ها و دکمه‌های بات.
 *
 * ⚠️ اینجا هیچ صفحه‌کلید reply‌ای نیست. بات در گروه کار می‌کند و صفحه‌کلید
 * reply هم مشترک است هم با privacy mode دیده نمی‌شود. همه‌چیز با دستور
 * (/report ، /month ، /me) و دکمه‌ی شیشه‌ای انجام می‌شود؛ دکمه‌ی شیشه‌ای در
 * گروه درست کار می‌کند و callback_query شناسه‌ی فشاردهنده را همراه دارد،
 * پس می‌دانیم دکمه را کدام عضو زده است.
 */

/**
 * فهرست دستورها برای منوی تلگرام.
 *
 * با `setMyCommands` ثبت می‌شود تا اعضا مجبور نباشند دستور را از حفظ تایپ
 * کنند؛ با زدن «/» یا دکمه‌ی منو همین فهرست بالا می‌آید.
 * ⚠️ هر دستوری که اینجا هست باید در `src/bot/index.ts` هندلر داشته باشد
 * (تست `tests/commands.test.ts` همین را می‌سنجد).
 */
export const COMMANDS = [
  { command: "report", description: "گزارش امروز من" },
  { command: "close", description: "پایان روز من" },
  { command: "month", description: "خروجی اکسل ماهانه" },
  { command: "undo", description: "برگرداندن آخرین ثبت من" },
  { command: "members", description: "اعضای ثبت‌شده" },
  { command: "me", description: "نام و سمت من" },
  { command: "start", description: "راهنما" },
] as const;

/**
 * راهنمای قالب گزارش — همان متنی که در گروه پین می‌شود.
 * هم /start و /help همین را می‌دهند، هم پیام صبحگاهی.
 */
export const GUIDE =
  "📋 راهنمای گزارش روزانه\n\n" +
  "همان‌طور که حرف می‌زنید بنویسید. قالب خاصی حفظ نکنید؛ فقط این چهار چیز " +
  "برسد: کِی، کجا، چه کاری، تا کِی.\n\n" +
  "━━━━━━━━━━━━━━━━━━━━\n" +
  "۱️⃣ شروع روز\n" +
  "«ساعت ۹ اومدم پروژه همت»\n\n" +
  "۲️⃣ کاری که انجام دادید — هر وقت خواستید، چند بار\n" +
  "«صورت وضعیت رو رسیدگی کردم»\n" +
  "«پیگیری کارهای ساختمان آمود»\n\n" +
  "۳️⃣ رفتن به محل بعدی\n" +
  "«ساعت ۱۲ رفتم باغ موزه»\n\n" +
  "۴️⃣ پایان روز\n" +
  "«ساعت ۱۹ رفتم»  یا  «الان تعطیل کردم»\n" +
  "━━━━━━━━━━━━━━━━━━━━\n\n" +
  "🔸 گزارش برای دیگری: پیام را با نام او شروع کنید —\n" +
  "«آیدین امروز ساعت ۷ رفت باغ موزه تا ۵ اونجا لوله‌کشی کرد»\n\n" +
  "🔸 ویس هم می‌شود فرستاد؛ ساعت را واضح بگویید.\n\n" +
  "🔸 اشتباه شد؟ /undo آخرین ثبتِ شما را برمی‌دارد.\n\n" +
  "📌 نکته‌ها\n" +
  "• ساعت با رقم یا حرف، هر جور راحتید: «۹» ، «۹:۳۰» ، «نه و نیم».\n" +
  "• «۵ رفتم» یعنی ۱۷:۰۰ — بعدازظهر پیش‌فرض است.\n" +
  "• سلام و تشکر ثبت نمی‌شود، پس نگران شلوغی نباشید.\n" +
  "• پیام‌های ثبت‌شده با 👌 علامت می‌خورند؛ بات جواب اضافه نمی‌دهد.\n" +
  "• روزِ همه هر شب ساعت ۲۳:۳۰ خودکار بسته می‌شود.\n\n" +
  "دستورها\n" +
  "/report گزارش امروزِ من · /undo برگرداندن آخرین ثبت\n" +
  "/month اکسل ماهانه · /members اعضا · /me نام و سمت من";

export const MSG = {
  welcome:
    "سلام 👋 من گزارشِ فعالیت روزانه‌ی اعضای هیئت‌مدیره را می‌نویسم.\n\n" +
    GUIDE,



  askName: (suggestion: string, hasKnownNames: boolean) =>
    "قبل از ثبت گزارش، نام کامل شما را لازم دارم.\n" +
    (hasKnownNames
      ? "اگر یکی از نام‌های زیر شما هستید دکمه‌اش را بزنید، وگرنه نام کامل خود را بنویسید."
      : suggestion
        ? `نام تلگرام شما «${suggestion}» است. اگر درست است دکمه را بزنید، وگرنه نام کامل را بنویسید.`
        : "نام و نام‌خانوادگی خود را بنویسید."),

  askRole: (name: string) =>
    `ممنون ${name} 🙏\nسمت شما چیست؟ (مثلاً: مدیرعامل، عضو هیئت‌مدیره، مدیر فنی)`,

  profileDone: (name: string, role: string) =>
    `✅ ثبت شد: ${name} — ${role}\nاز این به بعد دیگر نمی‌پرسم؛ گزارش‌هایتان را بفرستید.`,

  pendingSaved: "گزارشی که قبل‌تر فرستاده بودید هم ثبت شد 👇",

  noProfile: "هنوز عضوی ثبت نشده. اولین گزارشتان را بفرستید تا نامتان را بپرسم.",

  notMyButton: "این دکمه برای عضو دیگری است.",

  noOpenDay: "امروز هنوز گزارشی از شما ثبت نشده.",
  nothingToUndo: "چیزی برای برگرداندن نیست.",
  undone:
    "↩️ آخرین ثبتِ شما برگردانده شد.\n" +
    "شرح‌ها از روی متن پیام‌ها بازسازی شدند؛ برای جمع‌بندی هوشمند دوباره /report بزنید.",
  noDataForMonth: "برای این ماه گزارشی ثبت نشده است.",
  noMonths: "هنوز داده‌ای برای گزارش‌گیری وجود ندارد.",

  dayClosed: (label: string) => `✅ روز «${label}» بسته شد.`,
  dayReopened: (label: string) =>
    `🔓 روز «${label}» بسته شده بود؛ با این گزارش دوباره باز شد.\n` +
    "در پایان دوباره /close را بزنید.",
  autoClosed: (labels: string[], who?: string) =>
    `ℹ️ روز${labels.length > 1 ? "های" : ""} ${labels.join("، ")}` +
    (who ? ` برای «${who}»` : "") +
    " هنوز باز بود؛ نهایی‌اش کردم و روز تازه را باز کردم.",

  goodMorning: (dateLabel: string) =>
    `☀️ صبح بخیر — ${dateLabel}\n` +
    "روزتان را شروع کنید: اولین پیام، ساعت و محلِ شروع را بگویید.\n\n" +
    GUIDE,

  /** جمع‌بندی شبانه — یک پیام به‌جای ده‌ها تأییدیه */
  nightSummary: (dateLabel: string, lines: string[]) =>
    `🌙 جمع‌بندی ${dateLabel}\n\n${lines.join("\n")}\n\n` +
    "روزها بسته شدند. اصلاحیه‌ها را فردا با /report ببینید.",

  busy:
    "⏳ همین حالا دارم روز شما را جمع‌بندی می‌کنم — چند لحظه صبر کنید.\n" +
    "دوباره زدن فقط کار را کندتر می‌کند.",

  onBehalf: (name: string) => `📝 به نام «${name}» ثبت شد.`,

  ambiguousName: (name: string, candidates: string[]) =>
    `🤔 «${name}» به چند نفر می‌خورد: ${candidates.join("، ")}.\n` +
    "برای اینکه اشتباه ثبت نشود، پیام را با نام کاملِ همان نفر دوباره بفرستید.",

  heard: (text: string) => `🎙️ شنیدم: «${text}»`,

  voiceTooLong:
    "🎙️ این ویس برای پیاده‌سازی خیلی بزرگ است.\n" +
    "کوتاه‌ترش کنید یا همان را تایپ کنید.",

  aiUnavailable:
    "⚠️ سرویس هوش مصنوعی الان جواب نداد.\n" +
    "گزارش از روی متنِ خودتان ساخته شد؛ اگر جایی کم‌وکاست دارد، همان را دوباره بنویسید.",

  aiIncomplete:
    "ℹ️ پاسخ هوش مصنوعی ناقص بود؛ زنجیره‌ی روز از روی متن پیام‌ها تکمیل شد.",

  voiceFailed:
    "❌ نتوانستم ویس را پیاده کنم. لطفاً همان را تایپ کنید.",

  processing: "⏳ در حال جمع‌بندی روز…",
  building: "⏳ در حال ساخت فایل اکسل…",
  notAllowed: "⛔️ دسترسی مجاز نیست.",
  error: "❌ خطایی رخ داد. لطفاً دوباره تلاش کنید.",
};

/**
 * دکمه‌های شناسایی — شناسه‌ی صاحبِ دکمه داخل داده است تا عضو دیگری نتواند
 * جای او را انتخاب کند.
 *
 * عضوهایی که دیگران برایشان گزارش داده‌اند («آیدین نوری») بالاتر از نام
 * تلگرام می‌آیند؛ وگرنه کسی که نام تلگرامش لاتین است دکمه را می‌زند و
 * یک هویتِ دومِ جدا از گزارش‌های قبلی‌اش ساخته می‌شود.
 */
export function identityKeyboard(
  userId: number,
  suggestion: string,
  unlinked: Array<{ id: number; fullName: string }>,
) {
  const kb = new InlineKeyboard();
  for (const m of unlinked.slice(0, 6)) {
    kb.text(`👤 ${m.fullName}`, `link:${userId}:${m.id}`).row();
  }
  if (suggestion) kb.text(`✅ «${suggestion}» درست است`, `name:${userId}`);
  return kb;
}

/** دکمه‌های زیر گزارش روزانه */
export function reportKeyboard(userId: number, dayId: number) {
  return new InlineKeyboard()
    .text("🔄 به‌روزرسانی", `refresh:${userId}:${dayId}`)
    .text("⏹️ پایان روز من", `close:${userId}:${dayId}`);
}

/** فهرست ماه‌ها برای خروجی اکسل */
export function monthKeyboard(months: Array<{ key: string; label: string }>) {
  const kb = new InlineKeyboard();
  for (const m of months) kb.text(m.label, `month:${m.key}`).row();
  return kb;
}
